# DEX smile — focused visual check

1. **Fixed:** happy idle/run mouth corners are raised symmetrically. The mouth
   is wider and less circular; skin, cavity, lip and teeth share the contour.
2. **Fixed:** shorter fangs use rounded lathed tips instead of pointed cones.
3. **Preserved:** eating uses the previous opening; tired expressions and
   breathing suppress the smile. No gameplay or physiology changes.
4. **Verification:** 1,260 geometry poses pass, with added smile symmetry,
   rounded-fang and unchanged-eating assertions. Browser integration passes
   both directions, jumping, all ten stages, equipment, BG expressions, eating
   and fallback without page errors. Inspected `pose-idle.png` in
   `tests/playwright/2026-09-06_dex-game/`.

The subjective character style remains open to the owner's visual feedback.
